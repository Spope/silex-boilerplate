{% extends 'layout.html.twig' %}


{% block main_container %}
    Ready : {{ready}}

    {% if demos is defined %}
        {% for demo in demos %}
            <p>{{demo.name}}</p>
        {% endfor %}
    {% endif %}
{% endblock %}
